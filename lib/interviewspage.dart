
import 'package:flutter/material.dart';

class InterviewsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This is the Interviews page.'),
    );
  }
}